Quick setup commands:

mkdir -p golf-swing-capture/capture
cd golf-swing-capture

Then create the files above.

Run with:

source /Users/root1/oak-env/bin/activate
pip install -r requirements.txt
python app.py --out /Volumes/Torrents/golf_takes

Open:

http://127.0.0.1:8000


Nice next repo upgrades

After this, I’d add:
	•	scripts/
	•	tests/
	•	docs/
	•	roof_recorder.py
	•	radar_import.py
	•	pyproject.toml
